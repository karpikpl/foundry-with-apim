name: 'Bicep: ai-gateway-openrouter'

on:
  push:
    branches: [main]
    paths:
      - 'infra/ai-gateway-openrouter/**'
      - 'infra/modules/**'
      - '.github/workflows/bicep-validate-reusable.yml'
  pull_request:
    branches: [main]
    paths:
      - 'infra/ai-gateway-openrouter/**'
      - 'infra/modules/**'
      - '.github/workflows/bicep-validate-reusable.yml'

jobs:
  validate:
    uses: ./.github/workflows/bicep-validate-reusable.yml
    with:
      bicep-file: main.bicep
      working-directory: infra/ai-gateway-openrouter
